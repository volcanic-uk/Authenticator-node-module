const Principal = require('../src/authenticator/v1/principals'),
    Identity = require('../src/authenticator/v1/identity'),
    Services = require('../src/authenticator/v1/services');
module.exports = {
    Principal,
    Identity,
    Services
};